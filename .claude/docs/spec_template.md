<!-- このファイルはClaude専用の構造テンプレートです。直接編集しないでください。 -->
<!-- Claudeがユーザーの要望をもとに @docs/spec.md を生成する際の雛形として使用します。 -->

# Specification: [アプリ名]

## 1. 概要 (Overview)
- **課題:** ユーザーが抱えている問題や不便。
- **解決策:** このアプリが提供する価値（1行で）。
- **対象ユーザー:** 誰が、どんな状況で使うか。

## 2. 競合分析 (Competitive Analysis)

### 主要競合サービス
| サービス名 | 特徴 | 参考にする点 | 差別化ポイント |
| :--- | :--- | :--- | :--- |
| [例: Todoist] | [例: シンプルなタスク管理] | [例: 自然言語入力] | [例: チーム機能が弱い] |

### テーブルステークス（必須機能）
この市場で競合と同等に戦うために最低限必要な機能。

- [例: タスクの作成・編集・削除 — 全競合が実装]
- [例: 期限設定とリマインド — 大半の競合が実装]

### 差別化戦略
競合が弱い or 未対応の領域で、このアプリが提供する独自の価値。

- [例: AI による自動タスク分解 — 競合で本格対応しているサービスなし]

## 3. ユーザーストーリー (User Stories)
主要なユーザー体験をストーリー形式で記述する。

- **US-1:** [ペルソナ]として、[行動]したい。なぜなら[理由/価値]だから。
- **US-2:** ...

## 4. データ構造 (Data Model)
Prisma Schema として定義するモデル。

| フィールド名 | 型 | 制約 | 備考 |
| :--- | :--- | :--- | :--- |
| `id` | String | Primary Key | cuid |
| `title` | String | Not Null | |

## 5. 機能要件 (Functional Requirements)
チェックリスト形式で、IDを振って管理する。

### コア機能（v1 必須）
- [ ] **REQ-1:** [例: ログイン済みユーザーのみが投稿できる]
- [ ] **REQ-2:** [例: 投稿内容は最大280文字でバリデーションする]

### 拡張機能（v1 任意 / v2 候補）
- [ ] **REQ-X1:** [例: タグによるフィルタリング] — 理由: [なぜ後回しにしたか]

## 6. UI/UX 要件 (UI/UX Requirements)

### グローバルレイアウト
- **共通レイアウト:** [例: 認証前はセンターレイアウト、認証後はサイドバー付き2カラム]
- **共有コンポーネント:** [例: ヘッダー（ロゴ、検索バー、通知アイコン、アバター）、サイドバー（ナビゲーション）、フッター]

### ロール別画面一覧
ロールごとにアクセス可能な画面を一覧化する。単一ロールの場合は1つのテーブルでよい。

| ロール | 画面名 | パス | 概要 |
| :--- | :--- | :--- | :--- |
| [例: User] | [例: ダッシュボード] | [例: /dashboard] | [例: 主要指標のサマリーと直近のアクティビティ] |
| [例: Admin] | [例: ユーザー管理] | [例: /admin/users] | [例: ユーザー一覧の検索・フィルタ・編集] |

### 画面別詳細仕様
各画面の表示要素・操作・レイアウトを記述する。機能要件（§5）の各REQがどの画面に対応するかを明示する。

#### [画面名]
- **パス:** [例: /projects/:id]
- **対象ロール:** [例: User]
- **レイアウト:** [例: 左にツリービュー、右にメインコンテンツエリア]
- **表示要素:**
  - [例: プロジェクト情報カード — タイトル、説明、ステータスバッジ、作成日]
  - [例: メンバーリスト — アバター、名前、ロール、最終アクティビティ]
  - [例: アクティビティタイムライン — 操作履歴を時系列で表示]
- **操作・インタラクション:**
  - [例: ステータスバッジクリックでステータス変更ドロップダウン表示]
  - [例: メンバー追加ボタンで招待ダイアログを開く]

### インタラクション仕様
標準UIコンポーネント（ボタン、フォーム等）では表現しきれない複雑なUI挙動を記述する。該当がない場合はこのセクションを省略してよい。

| 機能 | 挙動の詳細 |
| :--- | :--- |
| [例: ドラッグ&ドロップ並び替え] | [例: リスト項目の順序変更。ドロップ時にサーバーへ並び順を保存] |
| [例: リアルタイム更新] | [例: 他ユーザーの編集がWebSocketで即時反映。競合時はトースト通知] |
| [例: 無限スクロール] | [例: フィード画面で下端到達時に次の20件を自動読み込み] |

### データ可視化仕様
ダッシュボード等でグラフ・チャートを使用する場合に記述する。該当がない場合はこのセクションを省略してよい。

| 対象画面 | グラフ種別 | 表示内容 | 集計ロジック |
| :--- | :--- | :--- | :--- |
| [例: 分析ダッシュボード] | [例: 折れ線グラフ] | [例: 日別アクティブユーザー数] | [例: ログインログをUNIQUE user_id/日で集計] |
| [例: 分析ダッシュボード] | [例: 円グラフ] | [例: プラン別ユーザー比率] | [例: usersテーブルのplan列でGROUP BY] |

### ユーザーフロー
主要シナリオの画面遷移を記述する。
1. [例: トップページ → サインアップ → オンボーディング → ダッシュボード]
2. [例: ダッシュボード → 新規作成 → 編集 → 公開 → 共有]

### 空の状態・エッジケースのUI
- **Empty State:** [例: データ未登録時に「最初の○○を作成しましょう」CTAを表示]
- **エラー時:** [例: API失敗時にリトライボタン付きのエラーメッセージを表示]
- **ローディング:** [例: データ取得中はSkeleton表示]

## 7. エラーメッセージ定義 (Error Messages)
※ 表示方法（Toast/インライン等）は `rules/frontend/architecture.md` §8 に従う。
ここには機能固有のメッセージのみ記載する。

| 条件 | メッセージ |
| :--- | :--- |
| [例: title未入力] | [例: "タイトルは必須です"] |
| [例: ネットワークエラー] | [例: "通信に失敗しました"] |

## 8. 非機能要件 (Non-Functional Requirements)
- **レスポンシブ対応:** [モバイル対応が必要か、ブレークポイントの方針]
- **パフォーマンス:** [大量データの想定、ページネーション/無限スクロールの方針]
- **アクセシビリティ:** [最低限のキーボード操作対応、スクリーンリーダー対応等]

## 9. 受け入れ基準 (Acceptance Criteria)
「これができたら完了」というチェックリスト。

- [ ] `npm run build` が正常に完了すること
- [ ] `npm run test` が全てパスすること
- [ ] [例: 正常系の動作確認]
- [ ] [例: 異常系の動作確認]

## 10. 将来の拡張 (Future Considerations)
v1 では実装しないが、データ構造や設計で考慮しておく事項。

- [例: マルチテナント対応 — テーブルに `organizationId` を含めておく]
- [例: リアルタイム通知 — WebSocket導入時に備えてイベント設計を意識する]

## 11. 備考・制約 (Constraints/Notes)
- 外部API: [使用する場合のみ記載。例: Stripe, OpenAI API]
- その他の制約や注意事項
